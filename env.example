# محقق الفيديو الأصلي - متغيرات البيئة

# إعدادات الخادم
HOST=0.0.0.0
PORT=8000
DEBUG=False

# إعدادات الملفات
MAX_UPLOAD_SIZE=104857600  # 100MB
UPLOAD_TIMEOUT=300  # 5 دقائق
TEMP_DIR=./uploads

# إعدادات FFmpeg
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe

# عدد لقطات الشاشة
NUM_FRAMES=6

# إعدادات الأمان
ALLOWED_EXTENSIONS=mp4,avi,mov,mkv,flv,wmv,webm

# إعدادات الأداء
MAX_WORKERS=4
CACHE_ENABLED=True
CACHE_TTL=3600  # ساعة واحدة
